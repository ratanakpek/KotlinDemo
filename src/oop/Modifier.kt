package oop

/**
 * Created by r.pek on 04.10.2017.
 */
/*
* 1. public by default in front of var is has public
* 2. protected :
*    - its object can not call it father.isHandsome() or subclass
* 3. private can see only in its class even subclass can not see it*/